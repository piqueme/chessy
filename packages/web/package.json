{
  "name": "@chessy/web",
  "private": true,
  "packageManager": "yarn@3.0.2",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": ["build"],
  "scripts": {
    "build": "tsc -b --declaration",
    "clean": "rm -rf ./build && rm tsconfig.tsbuildinfo",
    "dev": "tsc -b -w --preserveWatchOutput"
  }
}
