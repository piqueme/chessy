describe('Piece', () => {
  test('should render', () => {
    expect(0).toEqual(0)
  })
})

// adds css overrides
// when no drag prop dragging does nothing
// triggers drag start when dragging
// size prop proxies to css
