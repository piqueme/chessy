{
  "name": "@chessy/web",
  "private": true,
  "packageManager": "yarn@3.0.2",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "yarn g:tsc -b",
    "clean": "rm -rf ./build && rm tsconfig.tsbuildinfo",
    "dev": "yarn g:tsc -b -w --preserveWatchOutput",
    "test": "yarn g:test --selectProjects @chessy/web"
  },
  "dependencies": {
    "@chessy/server": "workspace:packages/server"
  }
}
